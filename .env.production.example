# ============================================================================
# PRODUCTION ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to .env.production and fill in your actual values
# DO NOT commit .env.production to git (it's already in .gitignore)
#
# Usage:
#   cp .env.production.example .env.production
#   nano .env.production
# ============================================================================

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# PostgreSQL database settings
# Generate a strong password: openssl rand -base64 24
DB_USERNAME=postgres
DB_PASSWORD=CHANGE_THIS_STRONG_PASSWORD
DB_NAME=strategy
# SSL is disabled by default for Docker internal networks
# Set to 'true' only if using an external database service that requires SSL
DB_SSL=false

# ============================================================================
# REDIS CONFIGURATION
# ============================================================================
# Redis cache/rate limiting settings
# Generate a strong password: openssl rand -base64 24
REDIS_PASSWORD=CHANGE_THIS_STRONG_PASSWORD

# ============================================================================
# JWT AUTHENTICATION
# ============================================================================
# Secret key for signing JWT tokens
# Generate a strong secret: openssl rand -base64 32
# IMPORTANT: Use a strong, random secret in production!
JWT_SECRET=CHANGE_THIS_GENERATE_STRONG_SECRET

# ============================================================================
# FRONTEND CONFIGURATION
# ============================================================================
# Frontend URL - used for CORS and email links
# Option 1: Use IP address (for quick testing without domain)
FRONTEND_URL=http://YOUR_VPS_IP
# Example: FRONTEND_URL=http://206.189.91.220

# Option 2: Use domain (if you have one set up)
# FRONTEND_URL=https://yourdomain.com
# Example: FRONTEND_URL=https://example.com

# ============================================================================
# FRONTEND API URL (Build-time variable)
# ============================================================================
# This is used during frontend build to configure API endpoint
# Must match FRONTEND_URL (use http:// for IP, https:// for domain)
# Option 1: Use IP address
VITE_API_URL=http://YOUR_VPS_IP
# Example: VITE_API_URL=http://206.189.91.220

# Option 2: Use domain
# VITE_API_URL=https://yourdomain.com
# Example: VITE_API_URL=https://example.com

# ============================================================================
# LOGGING
# ============================================================================
# Log level: error, warn, info, debug
# Use 'info' for production, 'debug' for troubleshooting
LOG_LEVEL=info

# ============================================================================
# NOTES
# ============================================================================
# 1. Replace all CHANGE_THIS_* values with strong, randomly generated values
# 2. Generate passwords/secrets using:
#    - JWT_SECRET: openssl rand -base64 32
#    - DB_PASSWORD: openssl rand -base64 24
#    - REDIS_PASSWORD: openssl rand -base64 24
# 3. Replace YOUR_VPS_IP with your actual VPS IP address
# 4. If using a domain, uncomment the domain options and comment out IP options
# 5. DB_SSL=false is correct for Docker Compose (internal network, SSL not needed)
# 6. See solutions/vps-deployment.md for detailed setup instructions
# 7. See solutions/vps-domain-setup.md for domain and SSL setup
